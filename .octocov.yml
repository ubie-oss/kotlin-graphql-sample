# generated by octocov init
coverage:
  badge:
    path: docs/coverage.svg
push:
  if: true
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
report:
#  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
